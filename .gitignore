# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

.env.local
uploads/invoices/*
!uploads/invoices/.gitkeep
.mailmap

# IDE / editor
.vscode/
.cursor/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
.Spotlight-V100
.Trashes

# Python
__pycache__/
*.py[cod]
ai-service/venv/
ai-service/.env

# Claude / AI assistant
.claude/
.agents/
skills-lock.json
AGENTS.md
memory.md
CLAUDE.md

# Logs
*.log
logs/

# Temp
tmp/
temp/

# Throwaway demo helpers at the repo root — they hold plaintext seed passwords
# and are meant to be deleted once the demo is over, so they must never be
# swept into a commit by `git add .`
/seed-demo-users.ts
/check-demo-users.ts

# Design references (local input for a redesign pass, not part of the tracked
# repo — same convention as the 2026-07-30 Liquid Intelligence brief, see
# docs/CHANGELOG.md)
DESIGN.md
temporary_design_reference/